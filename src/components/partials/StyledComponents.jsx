import styled from 'styled-components';

export const Markdown = styled.div`
  max-width: 40rem;
  margin: auto;
  border-left: 1px solid #eee;
  padding-left: 1rem;

  p {
    line-height: 1.3rem;
  }
`;

export default {
  Markdown,
};
